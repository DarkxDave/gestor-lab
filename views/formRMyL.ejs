<% layout('partials/layout') %>
<h2>Formulario RM y L</h2>
<form method="get" action="/form-rmyl" class="row g-2 mb-3">
  <div class="col-auto">
    <input type="text" name="sample_id" class="form-control" placeholder="sample_id" value="<%= sampleId || '' %>" required>
  </div>
  <div class="col-auto">
    <button class="btn btn-outline-info" type="submit">Cargar</button>
  </div>
</form>
<%- include('partials/formTabs', { active: 'RM y L', sampleId }) %>
<div class="alert alert-secondary">Contenido RM y L (por definir).</div>
